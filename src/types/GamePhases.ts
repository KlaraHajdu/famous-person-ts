type GamePhases = {
    startGame: {
        component: JSX.Element
    }
    joinGame: {
        component: JSX.Element
    }
    waitingRoom: {
        component: JSX.Element
    }
    addNames: {
        component: JSX.Element
    }
    playGame: {
        component: JSX.Element
    }
    endGame: {
        component: JSX.Element
    }
}

export default GamePhases